# 춤붓 · Choomboot Publishing

춤붓 출판사 공식 홈페이지입니다. Astro 기반 정적 사이트로 한국어를 기본
언어로 제공하며 영어 페이지는 `/en/` 경로에서 제공합니다.

## 개발 실행

```bash
npm install
npm run dev
```

## 프로덕션 빌드

```bash
npm run build
npm run preview
```

빌드 결과는 `dist/`에 생성됩니다. 책 이미지와 다운로드처럼 그대로 배포할
정적 파일은 `public/`에서 관리합니다.
